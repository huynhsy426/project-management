version: '23.0.3'

services:
  node:
    container_name: node_server
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    port: 
      - 8082:8082
    volumes:
      - .:/app
    command: npm dev